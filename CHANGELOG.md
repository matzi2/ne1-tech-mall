# Changelog

배포할 때마다 버전을 올리고, 이 파일에 히스토리를 남깁니다.

## 0.2.3 — 2026-08-23

관리자 화면은 관리자 이메일 로그인에만 엽니다.

### 변경

- 운영화면·상품등록·노란 막대는 관리자 메일일 때만
- 다른 메일로 `/admin` 에 들어가도 마이페이지로 보냄
- 로그인 화면에 관리자 메일을 미리 넣지 않음

## 0.2.2 — 2026-08-23

개발 작업실을 쇼핑몰 화면에서 분리합니다.

### 변경

- 시놀로지·가비아·GitHub는 `/connect` 전용 작업실
- 쇼핑몰 헤더·푸터·운영막대·회사소개에서 개발 메뉴와 DNS 설정 표를 제거

## 0.2.1 — 2026-08-23

회사 공인 IP 아래 시놀로지 NAS에 쇼핑몰을 올리는 설정을 넣습니다.

### 추가

- `/connect/nas` 시놀로지 순서: 공유기 포트포워드, 컨테이너, 역방향 프록시, Let's Encrypt, 가비아 A
- `Dockerfile` / `docker-compose.yml` (내부 포트 43177)
- 회사 공인 IPv4를 넣으면 밖에서 80·443이 열리는지 확인

## 0.2.0 — 2026-08-23

개발용 작업창을 정리하고, 쇼핑몰 화면만 남깁니다.

### 변경

- 로그인·푸터는 쇼핑몰 화면. `/connect` 는 관리자만
- 가비아 대행 로그인 창 제거
- 무료 전용 공인 IPv4는 이 환경에서 발급하지 않음. A 레코드는 클라우드 호스팅 IP가 있을 때

## 0.1.9 — 2026-08-23

홈·제품몰을 홈쇼핑 순서로 이어서 주문까지 가게 합니다.

### 추가

- 홈: 오늘 추천, 품목 바로가기, 베스트·재고
- 제품몰 정렬(추천/가격/빠른 출고), 상품 바로 주문, 같은 품목
- www CNAME·_dmarc 반영 확인. 다음은 고정 호스팅 IP의 A 레코드

## 0.1.8 — 2026-08-23

호스팅 IPv4는 이 작업 서버에서 발급하지 않고, 가비아에 넣을 추가 DNS(_dmarc)를 정리합니다.

### 추가

- `_dmarc` TXT `v=DMARC1; p=none;` 추가 안내
- 임시 공개 주소(터널) 상태 표시. 고정 A 레코드는 호스팅 IP가 있을 때

## 0.1.7 — 2026-08-23

가비아 공식 로그인 화면을 작업창에 그대로 보여 줍니다.

### 추가

- `/connect/gabia` 에서 가비아 로그인·DNS 관리툴을 같은 Chrome 작업창에 표시
- 보스가 그 창에서 직접 로그인하고 www CNAME을 넣음

## 0.1.6 — 2026-08-23

가비아 해외 IP 인증번호 확인 후 로그인·DNS가 이어지지 않던 문제를 고칩니다.

### 변경

- 가비아가 준 `auth_token`을 올바른 위치에서 읽어 인증번호 확인
- 인증 성공 뒤에는 보안 문자 없이 로그인을 마무리하고 www CNAME 등록
- 인증 결과를 작업창에 크게 표시. 이전 인증번호는 다시 받지 않으면 쓰지 않음

## 0.1.5 — 2026-08-23

가비아 로그인을 작업창에서 받고, DNS를 등록합니다.

### 추가

- `/connect/domain` 가비아 아이디·비밀번호·보안 문자 입력
- 로그인 후 `www` CNAME `ne1-tech.co.kr.` 를 가비아 DNS에 등록
- 비밀번호는 서버 파일에 저장하지 않음

## 0.1.4 — 2026-08-23

가비아 DNS 설정을 작업창에서 진행합니다.

### 추가

- `/connect/domain` 가비아 DNS 관리툴 순서: 로그인 → DNS 관리툴 → 레코드 추가 → 저장
- 가비아 입력값: www CNAME `ne1-tech.co.kr.` (끝 마침표), A `@` 는 호스팅 IP
- 이미 있는 MX(Daum)·SPF·NS는 유지하고 다시 넣지 않음
- 레코드 한 줄 복사, 진행 체크, 공개 DNS 재조회

### 참고

- 가비아 로그인은 보안 문자가 있어 이 환경에서 계정에 직접 쓸 수 없습니다. 작업창 값으로 가비아에 넣습니다.

## 0.1.3 — 2026-08-23

공식 도메인과 DNS 정보를 쇼핑몰·운영화면에 넣습니다.

### 추가

- 공식 주소 `https://ne1-tech.co.kr` / `www.ne1-tech.co.kr` DNS 레코드표
- `/connect/domain` 에서 등록기관, 네임서버, A, CNAME, MX 저장
- 공개 DNS 조회(A/NS/MX/TXT/CNAME)로 현재 레코드 확인
- 가비아 네임서버(`ns.gabia.co.kr` 등)와 Daum 메일(MX `aspmx.daum.net`) 반영
- 운영화면·관리자 막대에서 도메인 상태 바로 열기
- 회사소개에 도메인·메일·DNS 안내

### 참고

- 호스팅 IPv4와 www CNAME은 아직 없습니다. IP가 정해지면 작업창에 넣고 가비아에 A·CNAME을 등록합니다.

## 0.1.2 — 2026-08-23

GitHub 저장소 연결과 관리자 운영화면을 배포합니다.

### 추가

- GitHub 저장소 `matzi2/ne1-tech-mall` 연결, `github` 원격으로 `main` 푸시
- 보이는 Chrome 작업창에서 사이트 연결 (팝업·숨은 브라우저 없음)
- 이메일 1회용 비밀번호 로그인. 고정 비밀번호는 서버에 저장하지 않음
- 로그인 세션 유지 (이 브라우저, 최대 30일)
- 관리자 계정 `matzi57@gmail.com` (정범)
- 운영화면 `/admin`: 주문·상품·회원·GitHub 상태를 항상 확인
- 관리자 로그인 시 상단 노란 막대에서 운영화면 상시 진입
- 버전 히스토리 `/history`

### 변경

- 쇼핑몰 로그인을 카카오보다 이메일 방식으로 먼저 진행
- 데모 비밀번호 `demo1234` 제거

## 0.1.1 — 2026-08-22

사이트 연결을 백그라운드가 아니라 작업 창으로 진행하도록 바꿨습니다.

### 추가

- `/connect` 사이트 연결 작업실: GitHub, 카카오, 디벨로퍼스, 카드, 송금, CI, 도메인, Origin
- GitHub 연결 설정: 저장소 이름·공개/비공개, 장치 코드 로그인 후 `github` 리모트로 푸시
- 카카오 MATCHDOC 흐름: `/oauth2/authorization/kakao` → `/redirect?accesstoken=`
- n-onetech.com 계열 CI (남색 `#000092`, 파랑 `#0046CA`, 빨강 `#E70000`, Pretendard)

## 0.1.0 — 2026-08-22

엔이원텍(NE1-TECH) 쇼핑몰 첫 공개 버전. 도메인 NE1-TECH.CO.KR 기준으로 구성했습니다.

### 추가

- 회사소개, 제품몰, 상품 상세, 장바구니, 주문 완료
- 상품 검색: 품번·제품명·사양·첨부 파일명, 헤더 자동완성
- 품목 분류: 배선용차단기, 누전차단기, 전자접촉기, 전원장치, 단자대, 서지보호기, 보조릴레이, 퓨즈·홀더
- 연락처: TEL 032-220-0823 · 0824, FAX 032-220-0926
- 로그인 / 회원가입 / 아이디·비밀번호 찾기 전용 화면
- 상품 등록: 카탈로그 파일(CSV/JSON), 제품 사진, 기술 문서
- 결제: 무통장 송금, 신용·체크카드
- 회원 구매 시 포인트 적립(실결제 100원당 1P) 및 주문 시 사용
